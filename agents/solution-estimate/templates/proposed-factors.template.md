---
output: Estimation-Proposed-Factors.md
schema-version: proposed-factors.v1
agent: solution-estimate
generated-by: /estimate
generated-when: "conditional — only when at least one requirement does not fit any factor in the 103-factor catalogue"
do-not-edit: "Generated by /estimate. User reviews + accepts permanently (added to factor-rates.yaml) or once."
generated-at: "<populated at run time>"
project: "<project-name>"
gate: "user-review-required"
proposed-factor-count: 0
---

# Proposed Factors — {project-name}

> **Conditional output.** Emitted only when `/estimate` encounters a requirement that does not fit any of the 103 factors in [factor-rates.yaml](factor-rates.yaml).
>
> **Decision required.** Each proposed factor below is awaiting user review. Three resolutions per proposal:
>
> 1. **Accept permanently** — merge into `factor-rates.yaml` and `factor-definitions.md`; future `/estimate` runs use it.
> 2. **Accept once** — apply to this estimate only; do not persist.
> 3. **Reject** — pick a different existing factor for the affected requirements OR mark them as `Out of Scope`.
>
> If `project.config.yaml estimation.factorReviewGate: true`, the final `Estimation-ModuleOverallHrs.md` is **blocked** until every proposal here is resolved.

## AI Summary

`/estimate` found {N} requirement(s) that did not match any of the 103 factors in the canonical catalogue. {M} new factor(s) proposed below. Stakeholder review required before sign-off on `Estimation-ModuleOverallHrs.md`.

---

## Proposed factor #{n}: {proposed-factor-name}

| Field | Value |
|---|---|
| Suggested category | {one of the 11 categories} |
| Affected requirements | {REQ-001, REQ-005, REQ-012} |
| Rationale for proposing | {why no existing factor fits; what's different} |
| Closest existing factors (considered + rejected) | {existing-factor-1} (rejected because {reason}); {existing-factor-2} (rejected because {reason}) |

### Suggested hour rates

| VS | S | M | C | VC |
|---:|---:|---:|---:|---:|
| {hrs} | {hrs} | {hrs} | {hrs} | {hrs} |

(Use `null` for a complexity level if the work pattern doesn't manifest at that level.)

### Suggested complexity criteria

| Level | Scope hint |
|---|---|
| VS | {what would qualify as VS for this factor} |
| S | {what would qualify as S} |
| M | {what would qualify as M} |
| C | {what would qualify as C} |
| VC | {what would qualify as VC} |

### How this proposal was inferred

`/estimate` walked the 103-factor catalogue for each affected requirement, computed best-match scores, and found no match exceeding the acceptance threshold. The rates above are inferred from sibling factors in the same category and the requirement's stated complexity scope.

### Decision

- [ ] Accept permanently (merge into factor-rates.yaml + factor-definitions.md)
- [ ] Accept once for this estimate (do not persist)
- [ ] Reject — use existing factor `<name>` instead
- [ ] Reject — mark affected requirements as Out of Scope

**Decided by:** {user-name or role}
**Decided on:** {date}
**Notes:** {free-form rationale}

---

<!-- Repeat the "Proposed factor #N" block for every new factor. -->

## Validation Notes

- A proposal is **resolved** when one of the four Decision checkboxes is checked AND `Decided by` + `Decided on` are filled.
- When all proposals are resolved AND any "Accept permanently" decisions have been merged into `factor-rates.yaml` + `factor-definitions.md`, the gate clears and `Estimation-ModuleOverallHrs.md` can be (re)generated.
- Re-running `/estimate` after merging accepted-permanently factors will pick them up and no longer surface the same requirement here.

## Cross-references

- [factor-rates.yaml](factor-rates.yaml) — canonical 103-factor catalogue (accept-permanently merges land here)
- [factor-definitions.md](factor-definitions.md) — per-complexity descriptions (accept-permanently descriptions land here)
- [Estimation-BusinessReqDetail.md](./Estimation-BusinessReqDetail.md) — source of the "no match found" rows
- [Estimation-ModuleOverallHrs.md](./Estimation-ModuleOverallHrs.md) — the deliverable this file gates (when `factorReviewGate: true`)

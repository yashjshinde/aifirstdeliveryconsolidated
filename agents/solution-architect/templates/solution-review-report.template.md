---
output: projects/{p}/_aggregator/architecture/solution-review-report.md
schema-version: solution-review-report.v1
agent: solution-architect
generated-by: /solution-review
do-not-edit: "Generated by /solution-review. Hand-edits will be overwritten on next run."
project: "<project-name>"
agents-reviewed: []
generated-at: "<populated at run time>"
total-findings: 0
blockers: 0
warnings: 0
info: 0
---

# Solution Review Report — {project-name}

> Cross-agent gap analysis per [constitution/02-aggregation-rules.md § /solution-review](../constitution/02-aggregation-rules.md). Reads per-agent FDDs / TDDs / blueprints and surfaces inconsistencies that cross agent boundaries.

## AI Summary

Reviewed {N agents}: {list}. Found {B blockers}, {W warnings}, {I info}. Most critical: {one-line description of the top finding}.

This report does NOT block any downstream command. Findings are surfaced for the affected agent(s) to resolve.

## Severity legend

- **BLOCKER** — Cross-agent state is broken. Continuing without resolution will produce incorrect behaviour.
- **WARNING** — Inconsistency is non-functional but stakeholders should be aware.
- **INFO** — Notable observation; may not need action.

## Finding categories

| Category | What it checks | Typical severity |
|---|---|---|
| `ENTITY-MISMATCH` | An integration TDD references entity name `Account` but the CE TDD defines `acme_account` | BLOCKER |
| `NFR-CONFLICT` | Two agents declare different targets for the same end-to-end NFR | BLOCKER / WARNING |
| `INTEGRATION-CONTRACT-DRIFT` | Consumer's expected schema differs from producer's declared schema | BLOCKER |
| `MISSING-UPSTREAM` | A domain agent declared in `agents-enabled` has no blueprint.md yet | WARNING (deferred work) |
| `OWNERSHIP-CONFLICT` | Two agents claim ownership of the same architectural concern (Principle 1 violation) | BLOCKER |
| `ORPHAN-COMPONENT` | A component referenced by no cross-agent contract AND produced by no upstream | WARNING / INFO |
| `UNRECONCILED-ADR` | Two agents cite contradictory ADRs without an overriding cross-agent ADR | BLOCKER |

## Findings

### F-001 — {short title}

| Field | Value |
|---|---|
| Category | `ENTITY-MISMATCH` |
| Severity | BLOCKER |
| Agents involved | d365-ce, integration |
| Files | `projects/{p}/d365-ce/tdd.md §3.2.1` (line 142), `projects/{p}/integration/tdd.md §5.4` (line 87) |
| Description | CE TDD declares the entity as `acme_lead` (publisher-prefixed); integration TDD references it as `Lead` (no prefix). The integration plugin will fail at runtime. |
| Suggested resolution | Either: (a) update integration TDD to use `acme_lead`; OR (b) declare an alias in CE TDD with rationale; OR (c) introduce a logical-name translation in the integration layer + document the translation in integration TDD §6. |

### F-002 — ...

(One block per finding.)

## Summary table

| ID | Category | Severity | Agents | One-line description |
|---|---|---|---|---|
| F-001 | ENTITY-MISMATCH | BLOCKER | d365-ce, integration | `acme_lead` vs `Lead` |
| ... | | | | |

## What this report does NOT cover

- **Within-agent design quality.** That's the agent's own self-check appendix on its FDD/TDD/blueprint.
- **Implementation defects.** That's discovered by `/implement` + the test plan execution.
- **Estimate accuracy.** That's `solution-estimate`'s domain.

## Cross-references

- Per-agent design docs reviewed: `projects/{p}/{agent}/blueprint.md`, `tdd.md`, `fdd.md`
- Unified blueprint: `solution-blueprint.md`
- Visual prototype: `solution-prototype/index.html`

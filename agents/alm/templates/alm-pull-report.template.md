---
report-target: alm-pull
project: "{project}"
artefact: "{artifact}"
alm-tool: "{ado|jira}"
overwrite: "{overwrite|fail-on-conflict}"
scope: "{scope}"
levels: "{levels}"
read-only-levels: "{read-only-levels}"
feature: "{feature-slug-or-all}"
mode: "{pull|dry-run}"
generated-at: "{generatedAt}"
generated-by: /alm pull
---

# /alm pull Report — {project} — {artefact}

> Generated by `/alm pull`. Mode: **{mode}**.

## Summary

| Outcome | Count |
|---|---|
| Pulled (created locally) | {pulledCreatedCount} |
| Pulled (replaced local) | {pulledReplacedCount} |
| Diff suppressed (overwrite) | {overwriteCount} |
| Conflicts | {conflictCount} |
| Skipped (unchanged) | {skippedCount} |
| Errors | {errorCount} |

ALM tool: **{ado|jira}** at `{adoOrg|jiraBaseUrl}/{project}`.

Levels pulled: **{levels}**.
Read-only levels (locked): **{read-only-levels}**.

## Pulled — created locally

> Items in ALM that did not exist locally; now in `work-items.yaml`.

| LocalUID (assigned) | Tier | Title | ALM Id | Source |
|---|---|---|---|---|
{pulledCreatedRows}

## Pulled — replaced local

> Items where ALM is the source of truth (overwrite mode). Local versions replaced.

| LocalUID | Tier | Title | ALM Id | Local hash before | ALM hash after |
|---|---|---|---|---|---|
{pulledReplacedRows}

## Conflicts (fail-on-conflict mode)

| LocalUID | ALM Id | Conflict reason | Action |
|---|---|---|---|
{conflictRows}

## Local-only items (not in ALM; kept)

> Flagged for next `/alm push`.

| LocalUID | Tier | Title |
|---|---|---|
{localOnlyRows}

## Errors

| ALM Id or local id | Step | Message |
|---|---|---|
{errorRows}

## Mermaid recovery

> Mermaid source recovered from preserved `<pre><code class="mermaid">` blocks.

| LocalUID | ALM Id | Recovered source bytes | Round-trip OK |
|---|---|---|---|
{mermaidRecoveryRows}

## Read-only enforcement

> Items in `read-only-levels` cannot be modified by downstream agents' `/plan` after this pull.

| Locked tier | Count of items locked |
|---|---|
{lockedRows}

---

_Report generated by `/alm pull`. See [`templates/alm-pull-report.template.md`](../templates/alm-pull-report.template.md)._

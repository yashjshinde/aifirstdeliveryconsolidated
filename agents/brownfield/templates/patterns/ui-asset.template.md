<!--
ui-asset.template.md - Pattern for UI-shaped artefacts.
Used by bindings: form, view, chart, dashboard, canvas-app (per app; screens nested), web-page, web-template,
                  custom-page, email-template, word-template, excel-template, mail-merge, ribbon-diff,
                  sitemap, content-snippet, fo-form, pcf-control, pbi-report (visuals).
-->
---
artifact: "{artifactType}"
platform: "{platform}"
instance: "{name}"
source-paths:
{sourcePathsList}
extracted-at: "{extractedAt}"
generated-by: brownfield-engine
binding: "{bindingPath}"
pattern: ui-asset
validators-run: "{validatorsRun}"
known-gaps: "{knownGapsList}"
---

# {DisplayName} — {name}

> {one-line purpose}. Source: {primarySourcePath}.

## §1 Identification

- **Name**: `{name}`
- **Type**: {artifactType}
- **Form / page type**: {subtype} (e.g., Main / QuickCreate / QuickView / Card / Canvas / WebPage)
- **Bound entity / data source**: {dataSource}
- **Display name**: {displayName}
- **Owner / Publisher**: {owner}

## §2 Structure

> Verbatim layout tree. No grouping — every tab / section / control listed.

{structureTree}

## §3 Controls / fields / visuals

| # | Control | Type | Label | Binding | Notes |
|---|---|---|---|---|---|
{controlRows}

## §4 Behaviour — events / handlers / formulas

| Event | Handler | Source path | Notes |
|---|---|---|---|
{eventHandlerRows}

### Power Fx formulas (Canvas only)

```powerfx
{powerFxFormulasVerbatim}
```

### JS bindings (CE forms only)

```javascript
{jsBindingsVerbatim}
```

## §5 Layout / styling

- **Theme**: {theme}
- **Responsive behaviour**: {responsiveBehaviour}
- **Accessibility**: {a11yFlags}

## §6 Personalisation / customisation

- **Default views** (forms with subgrids only): {defaultViews}
- **Filters available**: {filters}
- **Sort orders**: {sortOrders}

## §7 Cross-references

{crossRefsSections}

## §8 Source evidence

```xml
{verbatimSourceExcerpt}
```

_Source: `{sourcePath}` lines {startLine}-{endLine}_

## §9 Known gaps

{knownGapsTable}

---

_Doc generated by `brownfield` agent. Binding: [`{bindingPath}`]({bindingPath}). Pattern: `ui-asset`._

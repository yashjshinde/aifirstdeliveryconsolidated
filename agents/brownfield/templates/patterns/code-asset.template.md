<!--
code-asset.template.md - Pattern for code-shaped artefacts.
Used by bindings: plugin, plugin-step (per step), js-function (per function), cwa, class (X++),
                  function-per-trigger (Azure Function), durable-orchestrator, adf-data-flow, stored-proc.
-->
---
artifact: "{artifactType}"
platform: "{platform}"
instance: "{name}"
source-paths:
{sourcePathsList}
extracted-at: "{extractedAt}"
generated-by: brownfield-engine
binding: "{bindingPath}"
pattern: code-asset
validators-run: "{validatorsRun}"
known-gaps: "{knownGapsList}"
---

# {DisplayName} — {name}

> {one-line purpose from extracted summary or method signature}.

## §1 Identification

- **Name**: `{name}`
- **Namespace / Assembly**: `{namespace}`
- **Source path**: `{sourcePath}` ({lineRange})
- **Language**: {language}
- **Build target / Framework**: {frameworkVersion}

## §2 Signature

```{language}
{signature}
```

## §3 Invocation context

- **When fires**: {triggerNarrative}
- **Bound to**: {bindingTargets}
- **Stage / Mode** (CE plugins only): {stage} / {mode}
- **Trigger** (Azure Functions): {triggerType} on `{triggerBinding}`
- **Filter attributes** (CE plugin step only): {filterAttributes}
- **Pre-image / Post-image** (CE plugin step): {imageRegistrations}

## §4 Inputs

| Parameter | Type | Source | Description |
|---|---|---|---|
{parameterRows}

## §5 Logic

> Numbered steps reflecting source order. Branches captured explicitly. Verbatim excerpts in fenced code blocks where validators require evidence.

{logicSteps}

## §6 Side-effects / Dataverse / Azure operations

| Step | Operation | Target | Notes |
|---|---|---|---|
{sideEffectRows}

## §7 Error handling

| Failure mode | Caught by | Action |
|---|---|---|
{errorHandlingRows}

## §8 Returns / outputs

```{language}
{returnSchema}
```

## §9 Cross-references

{crossRefsSections}

## §10 Source evidence

```{language}
{verbatimExcerpt}
```

_Source: `{sourcePath}` lines {startLine}-{endLine}_

## §11 Known gaps

{knownGapsTable}

---

_Doc generated by `brownfield` agent. Binding: [`{bindingPath}`]({bindingPath}). Pattern: `code-asset`._

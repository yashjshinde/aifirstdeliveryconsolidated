<!--
integration-asset.template.md - Pattern for integration-shaped artefacts (whole-flow, end-to-end).
Used by bindings: logic-app (whole-flow), api-connection, adf-pipeline, sb-topic, sb-subscription,
                  sb-queue, eg-topic, eg-subscription, apim-product, apim-api, apim-operation,
                  apim-policy, custom-connector (actions), dual-write-config, virtual-entity-provider,
                  synapse-link-config, event-hub, integration-account.
-->
---
artifact: "{artifactType}"
platform: "{platform}"
instance: "{name}"
source-paths:
{sourcePathsList}
extracted-at: "{extractedAt}"
generated-by: brownfield-engine
binding: "{bindingPath}"
pattern: integration-asset
validators-run: "{validatorsRun}"
known-gaps: "{knownGapsList}"
---

# {DisplayName} — {name}

> {one-line summary}. Source: {primarySourcePath}.

## §1 Identification

- **Name**: `{name}`
- **Type**: {artifactType}
- **Direction**: {direction} (Inbound / Outbound / Bidirectional)
- **SKU / Tier**: {skuTier}
- **Region**: {region}
- **Resource id**: `{azureResourceId}`

## §2 Contract

### Request

```{requestFormat}
{requestSchema}
```

### Response

```{responseFormat}
{responseSchema}
```

### Status codes / outcomes

| Code | Meaning |
|---|---|
{statusCodeRows}

## §3 Authentication / authorisation

- **Auth mode**: {authMode}
- **Identity**: {identityType} (Managed Identity / Service Principal / API Key)
- **Token source**: {tokenSource}
- **Permissions required**: {permissionsRequired}

## §4 Topology

```mermaid
{topologyMermaid}
```

## §5 Retry / DLQ / Idempotency

- **Retry policy**: {retryPolicy}
- **Max attempts**: {maxAttempts}
- **Backoff strategy**: {backoff}
- **DLQ destination**: {dlqDestination}
- **Idempotency**: {idempotencyStrategy}

## §6 Throughput / NFR

- **Target P95 latency**: {targetP95Latency}
- **Target throughput**: {targetThroughput}
- **Concurrency**: {concurrency}
- **Rate limit**: {rateLimit}

## §7 Observability

- **App Insights workspace**: {appInsightsWorkspace}
- **Custom metrics**: {customMetrics}
- **Alert rules**: {alertRules}
- **Correlation propagation**: {correlationStrategy}

## §8 Network

- **VNet**: {vnet}
- **Private endpoints**: {privateEndpoints}
- **NSG rules**: {nsgRules}
- **Egress restrictions**: {egressRestrictions}

## §9 Cross-references

{crossRefsSections}

## §10 Source evidence

```{language}
{verbatimSourceExcerpt}
```

_Source: `{sourcePath}` lines {startLine}-{endLine}_

## §11 Known gaps

{knownGapsTable}

---

_Doc generated by `brownfield` agent. Binding: [`{bindingPath}`]({bindingPath}). Pattern: `integration-asset`._

<!--
security-asset.template.md - Pattern for security-shaped artefacts.
Used by bindings: security-role, fls-profile, team, table-permission, web-role, fo-role, duty, privilege,
                  security-key, hierarchy-security.
-->
---
artifact: "{artifactType}"
platform: "{platform}"
instance: "{name}"
source-paths:
{sourcePathsList}
extracted-at: "{extractedAt}"
generated-by: brownfield-engine
binding: "{bindingPath}"
pattern: security-asset
validators-run: "{validatorsRun}"
known-gaps: "{knownGapsList}"
---

# {DisplayName} — {name}

> {one-line scope}. Source: {primarySourcePath}.

## §1 Identification

- **Name**: `{name}`
- **Type**: {artifactType}
- **Effective for**: {applicabilityScope} (e.g., Org / BusinessUnit / Team / User)
- **Inherited from**: {parentRole}
- **Status**: {status}

## §2 Privileges matrix

> For role-style artefacts: the privilege matrix. For FLS profiles: the field-level allow/deny list.

| Entity / Field | Read | Write | Create | Delete | Append | Append-To | Share | Assign |
|---|:-:|:-:|:-:|:-:|:-:|:-:|:-:|:-:|
{privilegeRows}

## §3 Duties / privileges (FO-only)

| Duty | Privileges (count) | Mode |
|---|---|---|
{dutiesRows}

## §4 Members (team / web-role only)

| Member | Type | Membership type |
|---|---|---|
{membersRows}

## §5 Hierarchy security (hierarchy-security only)

- **Manager-chain depth**: {managerChainDepth}
- **Position hierarchy**: {positionHierarchy}
- **Custom hierarchy entity**: {hierarchyEntity}

## §6 Table permissions (web-role / table-permission)

| Table | Scope | Privileges | Inherited from |
|---|---|---|---|
{tablePermissionRows}

## §7 Cross-references

{crossRefsSections}

## §8 Source evidence

```xml
{verbatimSourceExcerpt}
```

_Source: `{sourcePath}` lines {startLine}-{endLine}_

## §9 Known gaps

{knownGapsTable}

---

_Doc generated by `brownfield` agent. Binding: [`{bindingPath}`]({bindingPath}). Pattern: `security-asset`._

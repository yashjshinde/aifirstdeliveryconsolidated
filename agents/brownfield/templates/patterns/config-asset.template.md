<!--
config-asset.template.md - Pattern for configuration-shaped artefacts (declarative settings).
Used by bindings: number-sequence, env-variable, connection-reference, currency, uom, auto-number,
                  audit-config, holiday-schedule, apim-named-value, country-feature, address-book.
-->
---
artifact: "{artifactType}"
platform: "{platform}"
instance: "{name}"
source-paths:
{sourcePathsList}
extracted-at: "{extractedAt}"
generated-by: brownfield-engine
binding: "{bindingPath}"
pattern: config-asset
validators-run: "{validatorsRun}"
known-gaps: "{knownGapsList}"
---

# {DisplayName} — {name}

> {one-line purpose}. Source: {primarySourcePath}.

## §1 Identification

- **Name**: `{name}`
- **Type**: {artifactType}
- **Scope**: {scope}
- **Owner / Publisher prefix**: {ownerPrefix}

## §2 Configuration values

| Key | Value | Effective from | Notes |
|---|---|---|---|
{configRows}

## §3 Defaults + bounds

- **Default**: `{defaultValue}`
- **Min**: {minValue}
- **Max**: {maxValue}
- **Allowed values**: {allowedValues}

## §4 Usage

> Where this configuration is consumed (mechanically computed from cross-refs).

{usageList}

## §5 Cross-references

{crossRefsSections}

## §6 Source evidence

```xml
{verbatimSourceExcerpt}
```

_Source: `{sourcePath}` lines {startLine}-{endLine}_

## §7 Known gaps

{knownGapsTable}

---

_Doc generated by `brownfield` agent. Binding: [`{bindingPath}`]({bindingPath}). Pattern: `config-asset`._

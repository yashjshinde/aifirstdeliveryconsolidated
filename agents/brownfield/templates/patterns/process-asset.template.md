<!--
process-asset.template.md - Pattern for process-shaped artefacts (workflows, flows, rules).
Used by bindings: classic-workflow, bpf, flow (Power Automate cloud), business-rule, business-event,
                  dup-detection-rule, auto-resolution-rule, routing-rule, logic-app-consumption,
                  logic-app-standard, fo-workflow, batch-job, er-mapping.
-->
---
artifact: "{artifactType}"
platform: "{platform}"
instance: "{name}"
source-paths:
{sourcePathsList}
extracted-at: "{extractedAt}"
generated-by: brownfield-engine
binding: "{bindingPath}"
pattern: process-asset
validators-run: "{validatorsRun}"
known-gaps: "{knownGapsList}"
---

# {DisplayName} — {name}

> {one-line purpose}. Source: {primarySourcePath}.

## §1 Identification

- **Name**: `{name}`
- **Type**: {artifactType}
- **Scope**: {scope}
- **Primary entity / target**: {primaryEntity}
- **Owner**: {owner}
- **Status**: {status} (Active / Draft / Suspended)

## §2 Trigger

- **Type**: {triggerType} (Automatic / Manual / Scheduled / OnDemand / EventGrid / ServiceBus)
- **Event**: {triggerEvent}
- **Conditions**: {triggerConditions}
- **Filter expression**: `{triggerFilter}` (verbatim)

## §3 Steps / Actions / Stages

> Numbered to match source order. Branches captured explicitly. Loops and parallel scopes are own sub-sections.

{stepsNumbered}

### Branches

| Step | Condition | True branch | False branch |
|---|---|---|---|
{branchRows}

### Loops / Parallel scopes

{loopParallelDetail}

## §4 Approvers / Stages (workflow-only)

| Stage | Approver(s) | Type | Delegation | Escalation |
|---|---|---|---|---|
{approverRows}

## §5 Output / side-effects

| Step | Operation | Target | Body |
|---|---|---|---|
{sideEffectRows}

## §6 Error handling

| Failure mode | Caught by | Action |
|---|---|---|
{errorHandlingRows}

## §7 Cross-references

{crossRefsSections}

## §8 Source evidence

```{language}
{verbatimSourceExcerpt}
```

_Source: `{sourcePath}` lines {startLine}-{endLine}_

## §9 Known gaps

{knownGapsTable}

---

_Doc generated by `brownfield` agent. Binding: [`{bindingPath}`]({bindingPath}). Pattern: `process-asset`._

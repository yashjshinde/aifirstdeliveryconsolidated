<!--
container-asset.template.md - Pattern for container-shaped artefacts (group siblings under a parent).
Used by bindings: solution, patch, solution-dependency, fo-model, function-app, workspace,
                  model-driven-app, power-pages-site, adf-linked-service, canvas-component-library,
                  pbi-workspace, pbi-app.
-->
---
artifact: "{artifactType}"
platform: "{platform}"
instance: "{name}"
source-paths:
{sourcePathsList}
extracted-at: "{extractedAt}"
generated-by: brownfield-engine
binding: "{bindingPath}"
pattern: container-asset
validators-run: "{validatorsRun}"
known-gaps: "{knownGapsList}"
---

# {DisplayName} — {name}

> {one-line role of this container}. Source: {primarySourcePath}.

## §1 Identification

- **Name**: `{name}`
- **Display name**: {displayName}
- **Type**: {artifactType}
- **Version**: {version}
- **Layer / Tier**: {layerTier}
- **Publisher / Owner**: {ownerOrPublisher}

## §2 Contents / members

> Every artefact contained, listed individually. No grouping.

| # | Member | Type | Schema name | Doc link |
|---|---|---|---|---|
{memberRows}

## §3 Dependencies

### Required-by (this container is required by)

| Container | Reason |
|---|---|
{requiredByRows}

### Requires (this container requires)

| Container | Reason |
|---|---|
{requiresRows}

## §4 Versioning + history

| Version | Released | Notes |
|---|---|---|
{versionHistoryRows}

## §5 Configuration / environment

- **Environment-specific settings**: {envSettings}
- **Connection references**: {connectionReferences}
- **Environment variables**: {environmentVariables}

## §6 Permissions / access

| Principal | Role / Permission |
|---|---|
{permissionRows}

## §7 Cross-references

{crossRefsSections}

## §8 Source evidence

```{language}
{verbatimSourceExcerpt}
```

_Source: `{sourcePath}` lines {startLine}-{endLine}_

## §9 Known gaps

{knownGapsTable}

---

_Doc generated by `brownfield` agent. Binding: [`{bindingPath}`]({bindingPath}). Pattern: `container-asset`._

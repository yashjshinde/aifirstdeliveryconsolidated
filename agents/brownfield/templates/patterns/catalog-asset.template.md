<!--
catalog-asset.template.md - Pattern for catalog-shaped artefacts (master / reference data + business-domain catalogues).
Used by bindings: product, price-list, discount-list, territory, goal, marketing-list, campaign,
                  kb-category, sla, queue, entitlement.
-->
---
artifact: "{artifactType}"
platform: "{platform}"
instance: "{name}"
source-paths:
{sourcePathsList}
extracted-at: "{extractedAt}"
generated-by: brownfield-engine
binding: "{bindingPath}"
pattern: catalog-asset
validators-run: "{validatorsRun}"
known-gaps: "{knownGapsList}"
---

# {DisplayName} — {name}

> {one-line description}. Source: {primarySourcePath}.

## §1 Identification

- **Name**: `{name}`
- **Type**: {artifactType}
- **Effective from / to**: {effectiveFrom} -> {effectiveTo}
- **Owner**: {owner}
- **Status**: {status} (Active / Inactive / Draft)

## §2 Attributes

| Attribute | Value | Notes |
|---|---|---|
{attributeRows}

## §3 Members / line items (when applicable)

> Per-item enumeration. No grouping.

| # | Item | Attributes | Notes |
|---|---|---|---|
{lineItemRows}

## §4 Hierarchy / parent-child (catalog hierarchies)

```
{hierarchyOutline}
```

## §5 Cross-references

> Where this catalogue is referenced (mechanically computed).

{crossRefsSections}

## §6 Source evidence

```xml
{verbatimSourceExcerpt}
```

_Source: `{sourcePath}` lines {startLine}-{endLine}_

## §7 Known gaps

{knownGapsTable}

---

_Doc generated by `brownfield` agent. Binding: [`{bindingPath}`]({bindingPath}). Pattern: `catalog-asset`._

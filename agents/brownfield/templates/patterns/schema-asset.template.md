<!--
schema-asset.template.md - Pattern template for schema-shaped artefacts.
Used by bindings: entity, table, table-extension, edt, base-enum, data-entity, composite-entity,
                  aggregate-measurement, kpi, custom-api, cosmos-container, sql-database, sql-table,
                  pbi-dataset, custom-connector (schema definitions).
Rendered by brownfield-engine/pattern-renderer.ts.
-->
---
artifact: "{artifactType}"
platform: "{platform}"
instance: "{name}"
source-paths:
{sourcePathsList}
extracted-at: "{extractedAt}"
generated-by: brownfield-engine
binding: "{bindingPath}"
pattern: schema-asset
validators-run: "{validatorsRun}"
known-gaps: "{knownGapsList}"
---

# {DisplayName} — {name}

> {one-line description from binding.displayName}. Source: {primarySourcePath}.

## §1 Identification

- **Schema name**: `{schemaName}`
- **Display name**: {displayName}
- **Type**: {artifactType}
- **Namespace / Owner**: {namespace}
- **Source path**: `{sourcePath}` ({extractedFromLineRange})

## §2 Structure — fields

> {fieldCount} fields enumerated below. Source count verified: `{sourceFieldCount}` ({fieldCountMatch}).

| # | Schema name | Display name | Data type | Required | Default | Description |
|---|---|---|---|---|---|---|
{fieldRows}

## §3 Keys and indexes

### Primary key
- **Key**: `{primaryKey}`

### Alternate keys
{alternateKeysList}

### Indexes
{indexesList}

## §4 Relationships

### Outgoing (this entity references)

| Type | Target | Cardinality | Cascading | Source attribute |
|---|---|---|---|---|
{outgoingRelationships}

### Incoming (other entities reference this)

| Source | Type | Cardinality |
|---|---|---|
{incomingRelationships}

## §5 Lifecycle / behaviour

{lifecycleNarrative}

## §6 Cross-references (mechanically computed)

> Generated by `brownfield-engine/cross-ref.ts`. Listed in inventory order.

{crossRefsSections}

## §7 Source evidence

> Verbatim excerpts. Each block carries a source-path comment.

{sourceEvidenceBlocks}

## §8 Known gaps

{knownGapsTable}

---

_Doc generated by `brownfield` agent. Binding: [`{bindingPath}`]({bindingPath}). Pattern: `schema-asset`._

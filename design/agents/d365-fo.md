---
title: d365-fo — Dynamics 365 Finance & Operations
status: live
adr-refs: [ADR-0006, ADR-0001, ADR-0010]
last-reviewed: 2026-05-14
owner: design
---

# d365-fo — F&O autonomous

> Dynamics 365 Finance & Operations. Fundamentally different stack from CE — confirmed not to be a delta of d365-ce.

## Scope

- X++ classes (incl. CoC — Chain-of-Command extensions), tables, forms, form extensions, form parts
- AOT objects (maps, views, queries)
- Data Entities, composite entities, aggregate measurements, KPIs
- DMF projects (Data Management Framework)
- Batch jobs, recurring data jobs
- Electronic Reporting (ER) configurations
- F&O-native SSRS reports (RDP-driven; **not** CE SSRS — that's in the reporting agent)
- Business events, service classes (SOAP / JSON endpoints)
- Workflows (F&O workflow framework — distinct from CE classic workflows and Power Automate)
- Security keys, duties, privileges, role assignments
- Number sequences
- Print management setups
- Retail extensions (channel, hardware profile, payment connector — when retail module is in scope)
- LCS deployment, deployable packages

## docScope

Per [ADR-0006](../adr/0006-doc-scope-domain-vs-feature.md): **feature** for `fdd`, `tdd`, `blueprint`. F&O extensions are discrete per feature (one feature → one set of object-type changes); per-feature docs map cleanly to that delivery shape and preserve the Microsoft FastTrack pattern.

Each feature gets its own document set under `projects/{p}/d365-fo/features/{f}/`: `spec.md`, `plan.md`, `fdd.md`, `tdd.md`, `blueprint.md`, `test-plan/`, `tasks/`, `reviews/`, `output/`.

## Constitution — ported verbatim from battle-tested predecessor

The d365-fo constitution is ported largely verbatim from a battle-tested predecessor delivery framework to preserve years of refinement.

```
agents/d365-fo/constitution/
├── 00-charter.md                          # SOURCE NEW (agent purpose & scope)
├── 01-architectural-principles.md         # PORTED — Extension-over-modification, config-first 5-level priority, minimum footprint, batch design, upgrade compatibility
├── 02-governance-and-objects.md           # PORTED — RACI, object category framework, complexity classification
├── 03-object-type-standards.md            # PORTED — 10 categories: Data Entities, Security, Power Platform, Retail, Workflows, Business Docs, Reports, Integrations, Extensions
├── 04-extension-coding-standards.md       # PORTED — Full X++ standards, 32-type extension catalogue, naming
├── 05-development-and-alm.md              # PORTED — Environments, DevOps, source control, release, testing, Key Vault for INT objects
├── 06-documentation-and-change.md         # PORTED — Mandatory artefacts per category, change control, non-negotiable principles
├── 07-alm-configuration.md                # PORTED — ALM tool, work item hierarchy, field/priority/status maps
└── 08-nfr-targets.md                      # PORTED — AOS response, batch throughput, entity import rates, availability, error rate targets
```

**Port adaptation per file:** add YAML frontmatter (`source-attribution: ported from predecessor d365-fo constitution; last-reviewed: 2026-05-14`). Content stays **verbatim** — preserving years of refinement.

## Templates — ported with light adaptation

```
agents/d365-fo/templates/
├── fdd.template.md                        # PORTED + frontmatter + AI Summary section
├── tdd.template.md                        # PORTED + frontmatter + AI Summary section
├── blueprint.template.md                  # PORTED
├── test-plan/                             # REFIT to multi-doc structure
│   ├── index.template.md                  # PORTED (top-level)
│   └── suite.template.md                  # PORTED
├── task.template.md                       # PORTED
├── spec.template.md                       # SOURCE NEW (no reference equivalent)
├── plan.template.md                       # SOURCE NEW
├── review-report.template.md              # SOURCE NEW
└── checklists/
    ├── fdd-review.checklist.md            # PORTED VERBATIM
    ├── tdd-review.checklist.md            # PORTED VERBATIM
    ├── spec-review.checklist.md           # SOURCE NEW
    ├── plan-review.checklist.md           # SOURCE NEW
    ├── blueprint-review.checklist.md      # SOURCE NEW
    └── test-plan-review.checklist.md      # SOURCE NEW
```

## F&O-specific authoring conventions (preserved from the port)

These conventions are F&O-only — not imposed on other agents. Each agent owns its platform conventions.

- **★ mandatory section markers** — gate downstream commands like `/tdd`; section must be filled before progressing
- **"Not Applicable" semantics** — explicit handling, never empty/blank
- **Object-ID prefixes** for FDD→TDD→task traceability:
  - `EXT-NNN` — Extensions
  - `BDC-NNN` — Business Documents
  - `OPR-NNN` — Operations / batch jobs
  - `INT-NNN` — Integration / service classes
  - `DEN-NNN` — Data Entities
  - `SEC-NNN` — Security
  - `WFL-NNN` — Workflows
- **Content Depth Rules** — e.g., "current business process must be full end-to-end, never a single sentence"
- **`<TBD>` convention** + §5.12 Issues/Open Items roll-up
- **Quality Checklist** at end of TDD (self-review enforcement)
- **Pseudocode requirement** on every method in §5.6 / §5.7
- **Review classifications:** BLOCKER / REQUIRED / WARNING

The checklists' review classifications (BLOCKER/REQUIRED/WARNING) still apply: via `/review` for spec, and **inline** at generation time for FDD/TDD/blueprint/test-plan per [ADR-0001](../adr/0001-review-scope-spec-only.md).

## Extra commands (beyond base 17)

- `/lcs-deploy` — generate deployable package, push to LCS
- `/dmf-package` — produce data package zip from DMF project definitions

Declared in `agents.yaml`:

```yaml
- name: d365-fo
  base-commands: true
  extra-commands: [lcs-deploy, dmf-package]
```

## Customization inventory

What the agent can produce. Each entry maps to a TDD section, a task-generator, and an output sub-folder.

- Tables (incl. extensions), EDTs, base enums
- Forms, form extensions, form parts
- Classes (incl. CoC), maps, views, queries
- Number sequences, security keys/duties/privileges
- Batch jobs, recurring data jobs
- Business events
- Data entities, DMF projects
- F&O-style SSRS reports (RDP-driven)
- Electronic reporting configurations
- Workflows (F&O workflow framework)
- Service classes (SOAP / JSON service endpoints)

## Work products

Same byproducts as CE but template content is the ported F&O FastTrack format. **All feature-scoped** (per docScope):

- `projects/{p}/d365-fo/features/{f}/spec.md`
- `projects/{p}/d365-fo/features/{f}/plan.md`
- `projects/{p}/d365-fo/features/{f}/fdd.md`
- `projects/{p}/d365-fo/features/{f}/tdd.md`
- `projects/{p}/d365-fo/features/{f}/blueprint.md`
- `projects/{p}/d365-fo/features/{f}/test-plan/`
- `projects/{p}/d365-fo/features/{f}/tasks/`
- `projects/{p}/d365-fo/features/{f}/reviews/spec-review.md`
- `projects/{p}/d365-fo/features/{f}/output/`

## References

- ADRs: [ADR-0006](../adr/0006-doc-scope-domain-vs-feature.md), [ADR-0001](../adr/0001-review-scope-spec-only.md), [ADR-0010](../adr/0010-templates-agent-owned.md)
- Cross-references: [03-agent-inventory.md](../03-agent-inventory.md), [04-workflow-gates.md](../04-workflow-gates.md)
- Backlog: `bk-026` (d365-fo agent design — this doc), `bk-025` (per-agent generic)

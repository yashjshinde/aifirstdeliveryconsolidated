# work-items.yaml
# Generated by /plan — do not edit manually. Re-run /plan to regenerate.
# uid and alm-id fields are stable — /plan preserves them on re-run.
# Consumed by /alm extract and the external ALM Agent.

feature: {feature-name}
alm-tool: {alm-tool}           # from constitution/10-alm-configuration.md
generated-date: {YYYY-MM-DD}

work-items:

  # ── Level 1 — Reporting Capability (ALM type: Epic by default) ─────────────
  - id: EP-001
    uid: "{feature-name}-EP-001"   # stable unique identifier — never changes
    alm-id: null                   # set by /alm sync after ALM Agent creates the item
    level: 1
    alm-type: Epic
    title: "{Reporting capability title}"
    description: >
      {One to three sentences describing the reporting capability and business value delivered.}
    priority: High | Medium | Low
    fr-refs: [FR-001, FR-002]
    parent: null
    children: [FT-001, FT-002]

  # ── Level 2 — Functional Grouping (ALM type: Feature by default) ──────────
  - id: FT-001
    uid: "{feature-name}-FT-001"
    alm-id: null
    level: 2
    alm-type: Feature
    title: "{Functional grouping title — e.g., Dataset Build / Report Build / RLS Config}"
    description: >
      {One to two sentences describing the functional area — e.g., dataset tables and relationships,
      DAX measures, RLS configuration, or report pages.}
    priority: High | Medium | Low
    fr-refs: [FR-001]
    parent: EP-001
    children: [US-001]

  # ── Level 3 — Requirement Unit (ALM type: User Story by default) ──────────
  - id: US-001
    uid: "{feature-name}-US-001"
    alm-id: null
    level: 3
    alm-type: User Story
    title: "{User story title}"
    description: >
      As a {persona}, I want {reporting capability or insight}, so that {business value — decision enabled}.
    acceptance-criteria:
      - "AC-001: {Testable criterion — positive path}"
      - "AC-002: {Testable criterion — negative / RLS boundary / error case}"
    story-points: {1 | 2 | 3 | 5 | 8}
    priority: High | Medium | Low
    fr-refs: [FR-001]
    parent: FT-001
    children: [T-001, T-002]

  # ── Level 4 — Implementation Item (ALM type: Task by default) ─────────────
  - id: T-001
    uid: "{feature-name}-T-001"
    alm-id: null
    level: 4
    alm-type: Task
    title: "{Task title}"
    description: >
      {One to two sentences describing what must be built or configured.}
    acceptance-criteria:
      - "AC-001: {Testable criterion}"
    component-type: Dataset | DataModel | Measure | RLS | Report-Interactive | Report-Paginated | Report-SSRS | Dataflow | Configuration | Deployment | Testing
    complexity: S | M | L | XL
    priority: High | Medium | Low
    fr-refs: [FR-001]
    parent: US-001
    children: []
    status: TODO
    validation-status: NOT VALIDATED
    impl-doc-path: null

{
  "name": "agent-prompt-viewer",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "generate-data": "node scripts/generate-data.js",
    "build-html": "node scripts/build-html.js",
    "dev": "npm run generate-data && vite",
    "build": "npm run generate-data && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "marked": "^9.1.6",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.0",
    "vite": "^8.0.10"
  }
}

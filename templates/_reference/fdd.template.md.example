<!--
Scaffolding starter for agents/{agent}/templates/fdd.template.md.
Platform-specific shape lives in each agent's own templates folder (e.g., d365-ce uses
multi-file sub-platform packs assembled into _index per ADR-0005).
This starter shows the universal shape; agents replace it with their platform-shaped version.
-->
---
feature-id: {feature-slug}
agent: {agent-slug}
phase: DESIGN
schema-version: fdd.v1
gates-passed: [SPEC_APPROVED]
---

# Functional Design Document — {Feature Name}

<!-- TOC AUTO-GENERATED BY doc_lint -->
<!-- /TOC -->

## AI Summary

What this FDD covers + key design choices.

## 1. Introduction

### 1.1 Purpose

What functional design this document captures.

### 1.2 Scope

What's in. What's out. Spec out-of-scope items re-affirmed here.

### 1.3 Audience

Who reads this — business analysts, developers, testers, stakeholders.

### 1.4 Out of Scope

Cross-reference spec §7.

### 1.5 Open Questions roll-up

Aggregates open questions surfaced by sub-sections below. Resolved at `/clarify` time.

## 2. Process

### 2.1 Current process (as-is)

End-to-end. **Never a single sentence.** Diagram + narrative.

### 2.2 Future process (to-be)

End-to-end. Diagram + narrative.

### 2.3 Cross-Agent Dependencies

References `projects/{p}/_handoffs/` manifests where this feature's design depends on another agent.

## 3. User scenarios

Numbered. Each scenario references one or more spec FRs.

## 4. UI / Functional design

Platform-specific. The agent's `templates/fdd/{sub-platform}.template.md` sub-platform packs fill this section.

## 5. Reports

Platform-specific.

### 5.4 NFR mapping per feature

Each FR → NFR target.

## 6. Security

Privilege matrix + audit + hierarchy security. Detail per platform.

## 7. Entity / data model

Platform-specific. Diagram + detail tables.

## 8. Reference data

### 8.2 OOB-first Decision Log

Per requirement: OOB / Config / Customization, and why.

## 9. Multilingual considerations

Per `project.config.yaml multilingual`.

## 10. Handoffs to other agents

What this feature hands off and to whom. Manifests live in `projects/{p}/_handoffs/`.

## Appendix A — Acronyms

## Appendix B — User Story Reference

## Appendix C — References

## Appendix D — Glossary

## Quality self-check appendix

<!-- Populated inline by /fdd at end of generation per ADR-0001. doc_lint enforces presence. -->
